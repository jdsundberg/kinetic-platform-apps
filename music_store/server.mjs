/**
 * Music Store — No custom API endpoints.
 * Served as static files by the base server; proxy handles Kinetic API calls.
 */
export const appId = "music-store";
export const kapp = "music-store";
