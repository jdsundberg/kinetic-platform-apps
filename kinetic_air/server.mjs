/**
 * Kinetic Air — No custom API endpoints.
 * Served as static files by the base server; proxy handles Kinetic API calls.
 */
export const appId = "kinetic-air";
export const kapp = "kinetic-air";
