/**
 * Camp Registry — No custom API endpoints.
 * Served as static files by the base server; proxy handles Kinetic API calls.
 */
export const appId = "camp-registry";
export const kapp = "camp-registry";
