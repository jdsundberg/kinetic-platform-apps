/**
 * Asset Management — No custom API endpoints.
 * Served as static files by the base server; proxy handles Kinetic API calls.
 */
export const appId = "asset-management";
export const kapp = "asset-management";
